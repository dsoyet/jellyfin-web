name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths-ignore:
      - '**/*.md'

jobs:
  main:
    name: Build 🏗️
    uses: ./.github/workflows/__package.yml
    with:
      commit: ${{ github.sha }}
